#include "main.h"
/**
 * _abs - Entry point
 *
 * Description: a function that computes the absolute value of an integer.
 * @n: funcuntion parameter
 * Return: n or -n is Success
 */
int _abs(int n)
{
	if (n < 0)
	return (-n);

	else if (n >= 0)
		return (n);
return (0);
}
